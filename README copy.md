This is a Nextjs project uses as ui for an online shop.

## running the app

run the following commands:

```bash
docker compose up
```

Open [http://localhost](http://localhost) with your browser to see the result.
